# QueryLens
Scans incoming natural language queries for missing filters, unmapped schema attributes, ambiguous column references, and vague temporal logic (e.g., identifying whether "top customers" implies revenue, order frequency, or lifetime value).
